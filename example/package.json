{
  "name": "capacitor-image-marker-example",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && cap sync",
    "ios": "npm run cap:sync && cap open ios",
    "android": "npm run cap:sync && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@daboss2003/capacitor-image-marker": "file:.."
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "~5.4.0",
    "vite": "^5.2.0"
  }
}
